# Deploy Assets

Files:

- `backend.Dockerfile`
- `frontend.Dockerfile`
- `runner.Dockerfile`
- `runner-docker-proxy.Dockerfile`
- `docker-compose.yml`
- `backend.env.example`
- `k8s/README.md`

This folder contains the current local and deployment assets for the monolith backend, Next.js frontend, the isolated runner, the restricted Docker API proxy that fronts sandbox execution, PostgreSQL, and Redis.
