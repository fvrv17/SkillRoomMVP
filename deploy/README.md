# Deploy Assets

Files:

- `backend.Dockerfile`
- `frontend.Dockerfile`
- `runner.Dockerfile`
- `docker-compose.yml`
- `backend.env.example`
- `k8s/README.md`

This folder contains the current local and deployment assets for the monolith backend, Next.js frontend, isolated runner, PostgreSQL, and Redis.
