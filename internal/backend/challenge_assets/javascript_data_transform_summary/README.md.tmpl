Implement `summarizeCandidates` so candidate data is transformed into a deterministic summary.

Requirements:
- return total candidate count
- return a status breakdown
- return the top skills sorted by frequency, then name
- do not mutate the input collection
