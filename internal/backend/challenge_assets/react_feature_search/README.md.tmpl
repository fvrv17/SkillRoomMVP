Implement `{{component_name}}` with a debounced `{{input_label}}` input.

Requirements:
- debounce search updates by `delay`
- support the generated source contract for `{{candidate_source_name}}`
- handle sync arrays, paginated arrays, and async loaders
- apply the variant-specific matching rule from the task tests
- trim the query before filtering
- render the provided `emptyMessage`
- restore the full list when the query is cleared
