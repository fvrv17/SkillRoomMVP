Implement `{{component_name}}` so expensive metrics are memoized.

Requirements:
- render the provided summary fields
- recompute metrics when `items` change
- avoid recomputing metrics when unrelated props change
- keep the component API unchanged
