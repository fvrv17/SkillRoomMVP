Implement `{{component_name}}` so profile submission is validated and normalized.

Requirements:
- keep the component API unchanged
- export and use `validateProfile`
- export and use `normalizeProfile`
- reject short names, invalid emails, and invalid portfolio URLs
- submit trimmed values with a lower-cased email and an `https://` portfolio URL
