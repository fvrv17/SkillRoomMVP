Implement `{{component_name}}` so async member loading is reliable.

Requirements:
- load members whenever `teamId` changes
- show loading, error, empty, and success states
- ignore stale responses from older requests
- keep the component API unchanged
