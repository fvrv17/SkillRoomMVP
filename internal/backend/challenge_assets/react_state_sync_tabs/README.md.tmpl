Implement `{{component_name}}` so controlled tab state stays in sync.

Requirements:
- initialize from `activeId`
- sync local state when the parent changes `activeId`
- fall back to the first tab when the active tab disappears
- call `onChange` only for user-driven tab changes
