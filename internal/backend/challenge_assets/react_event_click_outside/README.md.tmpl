Implement `{{component_name}}` so outside clicks dismiss the panel safely.

Requirements:
- close on outside pointer interaction
- ignore clicks inside the panel
- clean up the global listener on rerender and unmount
- keep the component API unchanged
