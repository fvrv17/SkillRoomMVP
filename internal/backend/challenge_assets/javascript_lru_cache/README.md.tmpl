Implement `createLRUCache` so recency and eviction are deterministic.

Requirements:
- `set` stores a value and returns the cache instance
- `get` returns the value and refreshes recency
- `has` checks membership
- `entries` returns the current items from most recent to least recent
