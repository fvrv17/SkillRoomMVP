# Test Assets

This directory will hold cross-service tests and fixtures:

- end-to-end assessment scenarios
- contract test fixtures
- telemetry replay datasets
- load-test scenarios
- runner sandbox verification fixtures

Unit tests should still live beside the Go packages they verify.
